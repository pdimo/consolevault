# Node / Turborepo
node_modules/
dist/
.turbo/
*.tsbuildinfo
coverage/

# Env / secrets — NEVER commit
.env
.env.*
*.local

# Terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfplan
crash.log
# real values — commit ONLY terraform.tfvars.example (gitignore has NO inline comments)
terraform.tfvars
# tool-managed image refs (images.auto.tfvars) — per-install
*.auto.tfvars

# Google credentials — NEVER commit
*service-account*.json
*-key.json
gcp-key*.json
credentials.json
client_secret*.json
*oauth*client*.json

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
*.code-workspace

# Local-only working docs & reference material — never push to any remote
PLAN.md
temp-screens/
