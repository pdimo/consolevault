/** Stage 0 placeholder UI. The real single-admin management UI (SPEC §10) lands in Stage 4. */
export default function App() {
  return (
    <main style={{ fontFamily: 'system-ui, sans-serif', padding: '2rem' }}>
      <h1>ConsoleVault</h1>
      <p>Stage 0 — repo + Terraform bootstrap. The management UI arrives in Stage 4.</p>
    </main>
  );
}
